fastapi==0.115.0
uvicorn[standard]==0.30.0
sqlalchemy==2.0.35
pydantic==2.9.0
python-multipart==0.0.9
websockets==13.0

# PostgreSQL (Phase 1+)
# 운영 DB: PostgreSQL 16 + TimescaleDB extension
# pip install 후 DATABASE_URL 환경변수를 postgresql+psycopg://... 로 설정
psycopg[binary]==3.2.3
asyncpg==0.29.0
alembic==1.13.3

# MQTT bridge (Phase 1+)
asyncio-mqtt==0.16.2

# Cache / pub-sub (Phase 1+)
redis==5.1.1
